"""Test helpers for the DataCite metadata generator.

The project has no build step: `datacite_metadata_generator.html` is the
artifact and it contains a minified copy of `src/dmg.js`. Several tests
therefore inspect those two files as data, rather than importing them.
"""
